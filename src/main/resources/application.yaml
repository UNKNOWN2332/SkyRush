server:
  port: ${PORT:8080}

spring:
  application:
    name: SkyRush
  # Default: Render ichki PostgreSQL. Lokal: SPRING_R2DBC_URL / USERNAME / PASSWORD ni IDE yoki .env da localhost ga moslab qo‘ying.
  r2dbc:
    url: ${SPRING_R2DBC_URL:r2dbc:postgresql://dpg-d7kmqffavr4c73e1eu2g-a.oregon-postgres.render.com:5432/skyrush?sslMode=verify-full}
    username: ${SPRING_R2DBC_USERNAME:skyrush_user}
    password: ${SPRING_R2DBC_PASSWORD:vbD8eT8uqG4rmhEvduTZM4Ldy1crLrcX}
  sql:
    init:
      mode: always
jwt:
  secret: ${JWT_SECRET:juda_uzun_va_maxfiy_kalit_shu_yerda_bolishi_shart_32_belgi}
  expiration: 86400000 # 24 soat (ms da)

# Bir nechta origin: vergul bilan ajratiladi (OAuth / brauzer so‘rovlari uchun aniq origin kerak)
app:
  cors:
    allowed-origins: "${CORS_ALLOWED_ORIGINS:http://localhost:5173,https://skyrush-frontent.onrender.com}"

# Google Identity Services (Web) client ID — VITE bilan bir xil bo‘lishi kerak
google:
  oauth:
    client-id: ${GOOGLE_OAUTH_CLIENT_ID:691038508358-9fjig635aspra54lmsrrbnp5q70kcqdu.apps.googleusercontent.com}